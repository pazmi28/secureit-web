# Estado del proyecto — BytIA

> Bitácora compartida entre roles. Fuente de verdad de qué se ha hecho y qué queda abierto entre ejecuciones.
> Fase actual: pruebas de funcionamiento con 3 roles (Director, Desarrollo, Marketing — este último incorporado el 2026-09-15 en modo práctica). Ver `docs/sistema-agentes-roles-secureit.md`.

## Cómo se usa este fichero

- Cada rol añade una entrada nueva al final del bloque del día correspondiente (no edites entradas de otros roles ni de días anteriores).
- El Director lee este fichero al empezar cada ejecución y lo relee después de que cada rol operativo termine.
- Formato de entrada: qué se asignó/hizo, resultado, qué queda pendiente, y una nota si algo debería revisarlo un rol que todavía no existe (Diseño/CEO).

---

## 2026-09-14

### Director
- **Contexto:** Puesta en marcha del sistema de roles. Arranque en fase de pruebas con 2 roles activos (Director, Desarrollo); el resto de roles del diseño completo (`docs/sistema-agentes-roles-secureit.md`) quedan para más adelante.
- **Tarea asignada:** Housekeeping — (1) eliminar `SoporteTecnico.css` huérfano, (2) actualizar checklist "Pendiente — Desarrollo" de `CLAUDE.md` a la realidad del código.
- **Dispara a:** Desarrollo

### Director (cierre)
- **Resultado revisado:** confirmado con `git status`/`git diff` — `SoporteTecnico.css` y su carpeta eliminados, nota huérfana quitada de `CLAUDE.md`, checklist "Pendiente — Desarrollo" actualizado (10 ítems marcados `[x]`, quedan sin marcar solo "Test responsive mobile" y "Deploy en Vercel", correctamente porque no son verificables leyendo código). Build en verde.
- **Nota de proceso:** en esta ejecución el subagente `desarrollo` (`.claude/agents/desarrollo.md`) no apareció disponible por el tool Agent — probablemente porque la sesión ya estaba iniciada cuando se creó el fichero. Se ejecutó la tarea con el agente `general-purpose` usando el mismo prompt de rol como workaround. Pendiente de validar en un chat nuevo si `desarrollo` aparece ya en la lista de subagentes.
- **Sin encadenar:** ningún rol adicional activo en esta fase (Diseño/Marketing/CEO no existen todavía). Nota para cuando existan: nada relevante que revisar de esta tarea (housekeeping puro, sin cambios visuales ni de contenido).
- **Queda abierto:** cambios sin commitear (a la espera de que el usuario confirme si quiere que se commiteen), y validar en chat nuevo la disponibilidad del subagente `desarrollo`.

### Desarrollo
- **Hecho:** Housekeeping de dos tareas asignadas por Director. (1) Confirmado que ningún archivo importaba `src/components/SoporteTecnico/SoporteTecnico.css`; eliminado el archivo y la carpeta `SoporteTecnico/` (quedó vacía), y quitada la nota de huérfano correspondiente en `CLAUDE.md`. (2) Revisado `src/` y `src/App.js` contra el checklist "Pendiente — Desarrollo" de `CLAUDE.md`: marcados como hechos todos los ítems ya implementados y ensamblados (App.js limpio, variables CSS globales, useReveal.js, Navbar, Hero, Servicios con tabs + ServicioCard/SoporteTecnicoCard, DetalleServicio ×3, Nosotros, Contacto con formulario, Footer, ensamblaje en App.js); quedan sin marcar solo "Test responsive mobile" y "Deploy en Vercel", que no son verificables leyendo código.
- **Build/lint:** OK — `npm run build` (CI=true) compila con "Compiled successfully.", sin warnings de ESLint.
- **Pendiente:** Test responsive mobile y Deploy en Vercel (fuera del alcance de este housekeeping).
- **Nota para futuros roles:** ninguna

### Desarrollo
- **Hecho:** Prueba de verificación del flujo de roles Director→Desarrollo. Modificado el `<h1>` del Hero (`src/components/Hero/Hero.js`, línea 30-31): añadido " (prueba de roles)" al final del texto, después del punto, como texto plano dentro del mismo `<h1>`. El `<span>bytes</span>` se dejó intacto. No se tocó nada más de `Hero.js` ni de otros componentes.
- **Build/lint:** OK — `npm run build` compila con "Compiled successfully.", sin warnings nuevos.
- **Pendiente:** ninguno de esta tarea. El texto "(prueba de roles)" es temporal para la prueba del flujo; revertirlo cuando el Director lo indique.
- **Nota para futuros roles:** ninguna (cambio puramente de verificación, sin impacto visual/de contenido definitivo).

---

## 2026-09-15

### Desarrollo
- **Hecho:** Revertida la prueba de verificación del flujo de roles. En `src/components/Hero/Hero.js` (línea 30-31) se quitó el sufijo " (prueba de roles)" del `<h1>`, dejando el titular tal como estaba originalmente ("Pensamos en `bytes`, hablamos en soluciones."). No se tocó nada más del fichero. Confirmado en producción (Vercel) que el flujo Director→Desarrollo funcionó correctamente en la tarea anterior.
- **Build/lint:** OK — `npm run build` compila con "Compiled successfully.", sin warnings de ESLint.
- **Pendiente:** ninguno de esta tarea.
- **Nota para futuros roles:** ninguna.

### Director
- **Contexto:** El usuario pidió sustituir el logo genérico de texto del Navbar por el nuevo logo (`docs/logo_bytia.png`, referencia PNG con fondo negro sólido, aportada por el usuario), ajustando el tamaño.
- **Tarea ejecutada directamente** (sin encadenar al subagente `desarrollo`, por tratarse de un cambio acotado a un solo componente): copiado el PNG a `public/logo-bytia.png`; `Navbar.js` ahora renderiza `<img src="/logo-bytia.png" alt="BytIA">` en vez del wordmark de texto; `Navbar.css` envuelve el logo en una chip oscura (`.navbar-logo`, `background: var(--dark)`, `border-radius: 8px`) con `border`/`box-shadow` sutil solo en variante `.scrolled-light` para que tenga contraste sobre fondos claros — el fondo negro del PNG se funde con la chip en las variantes oscura/transparente. Imagen a `height: 40px` dentro de la chip.
- **Verificado visualmente** con `agent-browser` sobre el build de producción (`npm run build` + `serve`): navbar transparente sobre el hero, navbar oscuro (`.scrolled-dark`, sección Servicios) y navbar claro (`.scrolled-light`, sección Nosotros), más viewport mobile (390×844, con y sin menú hamburguesa abierto). En los tres estados y en mobile el tamaño y contraste de la chip se ven correctos.
- **Build/lint:** OK — `npm run build` (CI=true) compila con "Compiled successfully.".
- **Actualizado `CLAUDE.md`:** sección "Logo" — estado, ruta del archivo actual y nota de implementación sobre la chip (para revisar cuando llegue el vector SVG final).
- **Pendiente:** (1) el PNG pesa ~860KB, sin comprimir — no hay herramientas de imagen (ImageMagick/PIL/sharp) disponibles en este entorno para optimizarlo ahora; recomendable comprimirlo o sustituirlo por el SVG final antes de dar por cerrada la marca. (2) Vector final en Illustrator sigue pendiente (ya estaba en el checklist de marca de `CLAUDE.md`); cuando llegue, revisar si la chip de fondo oscuro del Navbar sigue haciendo falta (el SVG debería traer fondo transparente).
- **Nota para futuros roles:** ninguna (cambio de marca/UI, no hay roles de Diseño/Marketing activos todavía).
- **Sin commitear:** cambios en working tree a la espera de confirmación del usuario.

### Director
- **Contexto:** El usuario pidió incorporar el rol de Marketing para empezar a practicar cómo funcionaría el planning semanal de publicaciones de contenido, usando de forma explícita información de la web todavía no definitiva (servicios placeholder, stats/teléfono sin confirmar en `CLAUDE.md`).
- **Acción tomada:** Creado `.claude/agents/marketing.md` (rol Marketing, incluye SEO) siguiendo el mismo patrón que Director/Desarrollo. Actualizado `.claude/agents/director.md` para reconocer 3 roles activos y añadir lógica de encadenado hacia Marketing. Actualizado `docs/sistema-agentes-roles-secureit.md` (fase actual y checklist) y la cabecera de este fichero.
- **Tarea asignada:** generar un primer planning semanal de práctica en `docs/marketing/planning-semanal.md`, a partir de las secciones "Marca", "Servicios" y "Nosotros" de `CLAUDE.md`, marcando explícitamente qué datos son placeholder (stats, teléfono, servicios del grid aún por definir).
- **Dispara a:** Marketing

### Marketing
- **Hecho:** Primera ejecución del rol Marketing (práctica). Leído `CLAUDE.md` (secciones "Marca", "Servicios — detalle" y "Nosotros") y este fichero para conocer el estado real del proyecto. Generado el primer planning semanal de práctica en `docs/marketing/planning-semanal.md`: 5 publicaciones (lunes a viernes), canal principal asumido LinkedIn (con Instagram como secundario para la publicación del miércoles), cubriendo: presentación de marca, servicio de Soporte & Mantenimiento (CTA a WhatsApp real +34 631 019 668), stats de "Nosotros", sectores atendidos, y adelanto del catálogo de Ciberseguridad Empresarial.
- **Pendiente (a confirmar por el usuario antes de publicar contenido real):** (1) canal social definitivo (se asumió LinkedIn/Instagram sin decisión explícita); (2) stats de "Nosotros" (50+ clientes, 8+ años, 98% incidencias resueltas a la primera, 2h tiempo máximo de respuesta) — usadas tal cual pero marcadas como placeholder en `CLAUDE.md`, sin verificación externa; (3) teléfono de contacto (+34 976 000 000, placeholder explícito en `CLAUDE.md`) — no se ha usado en ninguna publicación, se dirigió el CTA a WhatsApp o al formulario web en su lugar; (4) catálogo definitivo del grid de Ciberseguridad Empresarial (Firewall Enterprise, Monitorización 24/7, Auditorías de Seguridad, Consultoría IT), marcado en el propio `CLAUDE.md` como placeholder pendiente de sustituir por los servicios reales.
- **Nota para futuros roles:** Diseño (cuando exista) debería revisar si necesita piezas gráficas simples para acompañar estas publicaciones (tarjeta de stats, grid 2×2 de servicios, carrusel de sectores) — no se ha producido ningún gráfico en esta tarea, solo el copy y el formato sugerido. Nada que Desarrollo deba ajustar en la web a raíz de este planning (no se promociona ninguna función no implementada).

### Director (cierre)
- **Resultado revisado:** `docs/marketing/planning-semanal.md` generado correctamente — 5 publicaciones con aviso de contenido de práctica en cabecera, cada una con red social, copy, CTA, formato y fuente/nota de trazabilidad a `CLAUDE.md`. Los datos placeholder (stats, teléfono, catálogo enterprise, canal social) quedan marcados de forma explícita y no se usó el teléfono placeholder en ningún CTA. No se tocó código de `src/` ni el checklist de `CLAUDE.md`.
- **Nota de proceso:** igual que ocurrió con `desarrollo` el 2026-09-14, el subagente `marketing` no apareció disponible por el tool Agent en esta misma sesión (se creó el fichero `.claude/agents/marketing.md` después de que la sesión ya estuviera iniciada). Se ejecutó con `general-purpose` pasando el prompt de rol completo como workaround. Pendiente de validar en un chat nuevo si `marketing` aparece ya en la lista de subagentes.
- **Sin encadenar:** Diseño y CEO siguen sin existir — la nota de Marketing sobre piezas gráficas queda registrada para cuando Diseño exista, sin actuar en su nombre. No hace falta encadenar a Desarrollo (el planning no promociona nada no implementado).
- **Queda abierto:** validar la disponibilidad de `marketing` como subagente en un chat nuevo; y antes de cualquier publicación real, que el usuario confirme canal social, stats verificadas, teléfono corporativo real y catálogo definitivo de servicios enterprise. Cambios sin commitear a la espera de confirmación del usuario.

---

## 2026-09-21

### Marketing
- **Contexto:** Investigación (no implementación) de conectores para pasar de copy en texto a pieza gráfica publicable, a petición del usuario. Comparadas: Canva Connect API/Autofill (requiere Enterprise), Canva MCP oficial (gratis en lo esencial), alternativas open source (HTML/CSS+Playwright, Satori), MCP de Buffer/Hootsuite, y la confusión de nombre entre higgsfield.ai (SaaS de vídeo/imagen, trial de 24h con tarjeta y auto-renovación) vs el repo `higgsfield-ai/higgsfield` en GitHub (framework de orquestación GPU no relacionado) vs `Open-Higgsfield-AI` (alternativa no oficial, MIT, self-hosted, gratis en local).
- **Hecho:** Detallado el catálogo de tools del Canva MCP oficial por plan — `generate-design`, `export-design`, `copy-design`, `upload-asset-from-url` funcionan en plan **Free**; `search-brand-templates`/`create-design-from-brand-template`/`resize-design` requieren Pro; `autofill-design` (relleno automático de plantilla de marca con variables) requiere **Enterprise**. Conclusión: con cuenta gratis se puede generar/exportar diseños por prompt y subir el logo de BytIA, pero sin garantía de fidelidad exacta a la paleta de marca en cada generación (eso exige plantilla de marca, bloqueada hasta Pro/Enterprise).
- **Acción del usuario:** activado el conector Canva en su cuenta (Settings → Connectors en claude.ai) y confirmado con `claude mcp list` desde esta sesión: `claude.ai Canva: https://mcp.canva.com/mcp - ✓ Connected` a nivel de cuenta/CLI.
- **Bloqueo detectado:** aun apareciendo "Connected" en `claude mcp list`, **las tools del conector Canva no cargaron en esta sesión de Claude Code** (comprobado con ToolSearch, sin resultados) — mismo síntoma que el problema ya documentado el 2026-09-14 con el subagente `desarrollo` recién creado: algo activado/creado con la sesión ya en marcha no se registra hasta abrir una sesión nueva.
- **Pendiente:** validar en un **chat nuevo** de Claude Code en este proyecto si las tools de Canva (`generate-design`, `export-design`, etc.) aparecen disponibles, y ejecutar ahí una primera generación de prueba (publicación del lunes de `docs/marketing/planning-semanal.md`) para ver el resultado real. Prompt de prueba entregado al usuario para esa sesión nueva.
- **Nota para futuros roles:** ninguna (verificación de herramienta, sin contenido publicado ni cambios de código).

### Marketing
- **Contexto:** Investigación (sin implementación) de dos herramientas propuestas por el usuario para el departamento de Marketing: "Arrow2 de QuiverAI" (herramienta de IA) y "Compositor" (editor de imágenes gratuito/open source), evaluando su posible encaje con el planning semanal de contenido de BytIA. Este rol no tiene acceso a WebSearch/WebFetch, así que la investigación se apoya únicamente en conocimiento interno (corte de conocimiento: enero 2026), sin poder verificar datos actuales en la web del proveedor.
- **Hecho — Arrow2 / QuiverAI:** No se ha podido identificar con confianza ninguna herramienta llamada "Arrow2" de un proveedor "QuiverAI" en el conocimiento disponible (no coincide con "Quiver", app de notas para desarrolladores, no relacionada con marketing/IA). Sin datos fiables sobre qué hace (generación de contenido, automatización de redes, analítica...), coste/modelo de precios, ni encaje real con BytIA — no se ha inventado ninguna característica. Conclusión: **necesita más investigación por parte del usuario** (verificar directamente en la web del proveedor o con un trial) antes de valorarla para el planning semanal.
- **Hecho — Compositor:** El nombre es ambiguo — no hay un único producto llamado exactamente "Compositor" identificable con certeza como editor de imágenes tipo Canva. Candidatos reales más cercanos: GIMP (editor raster completo, gratis/open source, sin plantillas de redes sociales, curva de aprendizaje alta); el módulo "Compositor" de Blender (nodo-based, gratis/open source, orientado a compositing de vídeo/VFX, no a banners rápidos de posts); Natron (similar, VFX). Sin confirmación de a cuál se refiere el usuario no se puede evaluar con precisión. Conclusión: **necesita más investigación por parte del usuario** — si es un compositor nodo-based (tipo Blender/Natron) previsiblemente NO sustituye a Canva para creatividades rápidas de posts (sin plantillas sociales, curva de aprendizaje alta); si es un editor raster tipo GIMP podría complementar (gratis y potente) pero tampoco trae plantillas de redes listas como Canva.
- **Pendiente:** el usuario debe aportar el enlace/proveedor exacto de ambas herramientas (especialmente "Arrow2 de QuiverAI", posible producto muy nuevo o de nicho no cubierto por el conocimiento del modelo) para poder evaluar coste, funciones reales y encaje con el planning semanal de BytIA.
- **Nota para futuros roles:** ninguna (investigación pura, sin cambios de contenido, código ni checklist).

### Marketing (actualización — datos verificados)
- **Contexto:** El usuario aportó el enlace oficial de Arrow2 (`https://quiver.ai/blog/introducing-arrow-2-0`), permitiendo sustituir la conclusión anterior ("necesita más investigación") por una evaluación con datos reales, obtenidos vía WebFetch sobre esa página.
- **Qué es Arrow2 (QuiverAI):** Herramienta de IA para generación de gráficos vectoriales e ilustraciones (SVG). Funciones: generación rápida de diagramas técnicos y lotes de diseños, geometría SVG más limpia (menos puntos de control), composición con espaciado/alineación automáticos, vectorización de imágenes raster a SVG editable, y micro-animaciones de elementos SVG para web. Dos modelos: **Arrow 2** (rápido/económico) y **Arrow 2 Telos** (proyectos complejos). Disponible en `app.quiver.ai`, con **acceso gratuito** para todos los usuarios (no se detalla en la página si hay límites de uso o plan de pago superior).
- **Encaje con BytIA:** Potencialmente útil como complemento a Canva, no como sustituto: BytIA necesita iconografía/diagramas técnicos (escudo de firewall, anillos de monitorización, paneles de estado — ver `DetalleServicio` en `CLAUDE.md`) y piezas de marca consistentes en SVG editable, que es justo el caso de uso de Arrow2 (vectorización + diagramas técnicos), a diferencia de Canva que es mejor para maquetación de posts con texto/plantillas sociales. Las micro-animaciones SVG podrían aprovecharse para contenido web (ej. reforzar los visuals de `DetalleServicio`) más que para redes sociales.
- **Conclusión revisada:** **útil como herramienta complementaria** para generar/vectorizar iconografía y diagramas técnicos de marca (gratis en su plan base), a validar en la práctica: (1) si el estilo generado encaja con la paleta/identidad de BytIA sin retoque manual pesado, (2) si el plan gratuito tiene límites de uso no indicados en la página. No sustituye una herramienta de maquetación de posts para redes (ese hueco lo sigue cubriendo Canva o similar).
- **"Compositor" (editor de imágenes open source):** sigue sin resolverse — el usuario no ha aportado aún enlace/proveedor concreto, se mantiene la conclusión anterior de "necesita más investigación".
- **Nota para futuros roles:** si se decide probar Arrow2 en la práctica, sería una tarea natural para un futuro rol de Diseño (cuando exista) o para Marketing al preparar piezas gráficas del planning semanal — no se ha generado ningún diseño de prueba en esta investigación.

### Marketing — Landing pages y dashboard de analítica (investigación, sin implementación)
- **Contexto:** El usuario (rol de negocio) pidió investigar dos temas relacionados y dar recomendación práctica, no solo teoría: (1) si tiene sentido crear landing page(s) independientes de la home actual, y (2) qué opciones existen para centralizar métricas de visitas/conversión de bytia.net. Investigación basada en conocimiento interno del modelo (sin WebSearch/WebFetch disponibles en este rol), sin implementar ni instalar nada.

**1) Landing pages — comparativa de opciones**

| Opción | Pros | Contras |
|---|---|---|
| (a) Anclas de la home actual (`#servicios`, `#contacto`) | Ya montado, coste cero, coherencia total de marca, cero mantenimiento extra | Mensaje genérico para tráfico de campaña específica (mismatch anuncio↔landing baja el Quality Score en Google Ads), difícil aislar conversión por servicio/campaña, URL poco memorable para un anuncio |
| (b) Nuevas rutas dentro de la misma SPA React | Reutilizaría componentes/diseño existentes | **Requiere React Router**, que `CLAUDE.md` prohíbe expresamente en esta fase ("NO instalar React Router — es una single page sin rutas"); un workaround manual sin router (renderizado condicional por `window.location.pathname`) complicaría la arquitectura actual sin necesidad real todavía. No recomendable en Fase 1 |
| (c) HTML estático suelto, fuera del router, en el mismo hosting Vercel (ej. una página aparte tipo `bytia.net/diagnostico-gratuito.html` o un subdominio) | No toca la SPA ni exige Router, coste cero (mismo Vercel), mensaje 100% enfocado a una sola oferta/keyword (mejor conversión y Quality Score), fácil de trackear con UTM aislados | Mantenimiento manual duplicado (HTML/CSS a mano, sin los componentes React reutilizables salvo que se copie el CSS de `App.css`), exige disciplina para no desincronizarse de la marca si esta cambia |
| (d) Herramientas externas (Unbounce, Instapage, Carrd...) | Rápidas para no-dev, A/B testing nativo, plantillas ya optimizadas para conversión | Coste mensual recurrente (Unbounce ≈80-90 USD/mes; Carrd es muy barato, ≈19 USD/año, pero limitado); depende de un proveedor SaaS externo; no reutiliza el sistema de diseño ya definido en `CLAUDE.md` salvo que se replique a mano |

- **Recomendación Fase 1 (sin cartera de clientes ni campañas de pago confirmadas todavía, según el propio `CLAUDE.md`):** seguir usando las anclas de la home (a) como está montado — no se justifica el esfuerzo/coste de una landing independiente sin una campaña de pago concreta detrás. No instalar React Router bajo ningún concepto en esta fase (contradice `CLAUDE.md` explícitamente).
- **Qué se plantearía en Fase 2 (cuando exista una campaña de pago concreta, ej. Google Ads "auditoría de seguridad Zaragoza" o Meta Ads para el lead magnet "diagnóstico gratuito" del Hero):** montar entonces landing(s) HTML estáticas sueltas (opción c), una por oferta/segmento, dentro del mismo proyecto Vercel — es la opción de menor coste y menor riesgo arquitectónico que sigue respetando la regla de "sin Router" de `CLAUDE.md`. Reservar herramientas externas de pago (opción d) solo si en el futuro se necesita A/B testing serio y hay presupuesto para ello; no tiene sentido en una pyme sin volumen de campañas aún.

**2) Dashboard de medición de accesos web — comparativa de opciones**

| Opción | Coste | RGPD / cookies | Notas |
|---|---|---|---|
| Google Analytics 4 | Gratis | Requiere banner de consentimiento de cookies (CMP) antes de cargar el script — usa cookies/identificadores; sin consentimiento válido hay riesgo de incumplimiento RGPD (AEPD) | Estándar de la industria, mejor integración con Google Ads (conversion tracking) si algún día hay campañas de pago en Google; curva de aprendizaje y configuración de eventos/conversiones no trivial para una pyme pequeña |
| Vercel Analytics | Gratis en el plan Hobby actual (con límites de volumen/retención), de pago si se escala | Basado en datos agregados, no usa cookies de tracking individual → no debería requerir banner de cookies | Nativo del mismo hosting ya usado, instalación mínima (paquete `@vercel/analytics`, unas pocas líneas) — pero menos granular: sin funnels ni segmentación de conversión avanzada |
| Plausible / Fathom / Simple Analytics | De pago (aprox. 9-15 USD/mes según proveedor y volumen; Plausible también existe self-hosted gratis pero exige mantener servidor propio, sin infra propia ahora mismo) | Privacy-first por diseño, no usan cookies ni datos personales → normalmente no requieren banner de consentimiento | Dashboards simples y legibles, buen equilibrio privacidad/funcionalidad, pero es un gasto SaaS recurrente nuevo |
| Google Search Console | Gratis | No trackea usuarios, no es analítica de tráfico general — no aplica cuestión de cookies | Complementario, no sustituye a las anteriores: mide rendimiento en buscadores (impresiones, clics, posición media, indexación) — imprescindible para el SEO local que corresponde a este rol |

- **Recomendación Fase 1 (pyme pequeña, sin gran presupuesto SaaS):**
  1. Dar de alta **Google Search Console** ya — gratis, sin implicación de cookies, y es la base para poder trabajar SEO local (Zaragoza) desde este mismo rol. Solo requiere verificar el dominio (DNS o meta tag), lo puede gestionar el usuario directamente.
  2. Activar **Vercel Analytics** (plan gratuito) como primera capa de analítica de tráfico — cero fricción, mismo proveedor de hosting, y al no usar cookies de tracking se evita montar un banner de consentimiento en esta fase. Esto sí implica instalar un paquete npm (`@vercel/analytics`), por lo que la decisión de ejecutarlo y el paquete en sí corresponden a Desarrollo, no a este rol.
  3. **Posponer GA4** hasta que haya una necesidad real de conversion tracking fino (típicamente cuando arranquen campañas de pago en Google Ads) — y en ese momento sí habrá que montar un banner de consentimiento de cookies (CMP) antes de cargar el script de GA4, no después, para cumplir RGPD.
  4. Plausible/Fathom quedan como opción de mejora futura (Fase 2) si en algún momento se quiere algo más granular que Vercel Analytics sin asumir la complejidad de consentimiento de GA4 — coste bajo pero es un SaaS nuevo, no urgente ahora.
- **Próximos pasos accionables:**
  - Confirmar con el usuario/Director si Fase 1 va a incluir campañas de pago a corto plazo (condiciona si merece la pena adelantar el trabajo de landing pages).
  - El usuario puede dar de alta Search Console ya mismo, sin depender de Desarrollo.
  - Cuando Desarrollo tenga hueco, valorar instalar `@vercel/analytics` como primera capa de medición (tarea de Desarrollo, no de Marketing).
  - Si se confirma una campaña de pago concreta, retomar entonces el diseño de una landing HTML estática dedicada (opción c) antes de considerar herramientas externas de pago.
- **Pendiente (decisiones que no corresponden a este rol):** decisión de negocio sobre si/cuándo lanzar campañas de pago (Google Ads / Meta Ads); decisión de Desarrollo sobre instalar `@vercel/analytics` (o, más adelante, el script de GA4 + CMP); presupuesto disponible para una herramienta privacy-first de pago si se quisiera evitar GA4 del todo.
- **Nota para futuros roles:** Desarrollo debería revisar si instalar `@vercel/analytics` es viable sin romper las reglas de stack de `CLAUDE.md` (es un paquete npm oficial de Vercel, ligero, no es una librería de UI/routing/animación de las prohibidas, pero la decisión de añadir cualquier dependencia nueva le corresponde a ese rol). Si en el futuro se monta una landing HTML estática (opción c), Diseño (cuando exista) debería revisar que reutiliza correctamente la paleta/tipografía de marca de `CLAUDE.md` para no crear una pieza visualmente desalineada de la home.

### Director
- **Contexto:** El usuario confirmó que próximamente habrá campañas de pago (Google Ads / Meta Ads), lo que activa el escenario "Fase 2" identificado por Marketing en la investigación anterior de este mismo día. Decisión: en lugar de esperar a tener la campaña 100% definida, conviene dejar ya montada una landing de pruebas para validar el circuito completo (diseño enfocado a una sola oferta, CTA de conversión, sin tocar la SPA) antes de que llegue tráfico de pago real.
- **Decisión tomada:** seguir la recomendación de Marketing — opción (c) de la comparativa: página HTML estática suelta, fuera del router de React, servida desde el mismo hosting Vercel (carpeta `public/`), sin instalar React Router (prohibido explícitamente en `CLAUDE.md` para esta fase). Oferta elegida para la landing de pruebas: el lead magnet "diagnóstico gratuito" ya mencionado en el Hero (`src/components/Hero/Hero.js`), por ser la oferta de menor fricción ya validada en el copy actual de la web.
- **Tarea asignada:** crear una landing de pruebas (`public/landing-diagnostico-gratuito.html` o nombre equivalente) — página estática independiente de la SPA, un único objetivo de conversión (solicitar el diagnóstico gratuito), reutilizando la paleta de colores y tipografía de marca definidas en `CLAUDE.md` (variables CSS, sin hardcodear colores), con CTA claro (WhatsApp +34 631 019 668 y/o mailto a hola@bytia.net, igual que en `SoporteTecnicoCard`). Marcar explícitamente en el propio HTML (comentario) y en `CLAUDE.md`/este fichero que es una landing **de pruebas**, pendiente de contenido/copy definitivo antes de usarla en una campaña real. No tocar ningún componente de `src/` ni el flujo de la SPA existente.
- **Dispara a:** Desarrollo

### Desarrollo
- **Hecho:** Creada la landing de pruebas `public/landing-diagnostico-gratuito.html` — página HTML estática suelta, fuera del router de la SPA React, servida directamente por Vercel desde `public/`. Un único objetivo de conversión (solicitar el diagnóstico gratuito ya mencionado en el botón principal del Hero), sin navbar ni navegación a otras secciones: solo cabecera con logo (`/logo-bytia.png`, enlazado a `/`), hero con eyebrow/título/subtítulo, dos CTA (WhatsApp `https://wa.me/34631019668?text=...` con mensaje precargado y `mailto:hola@bytia.net?subject=...&body=...`, mismo patrón que `src/components/Servicios/SoporteTecnicoCard.js`), un bloque breve de "qué incluye el diagnóstico" y un CTA final repetido, más un pie mínimo con email de contacto. Identidad de marca reutilizada declarando las mismas custom properties CSS que `src/App.css` (`--dark`, `--dark2`, `--blue`, `--blue-dark`, `--text-dark`, `--text-muted`, `--border`, `--font` system-ui) dentro del propio `<style>` del HTML, sin hardcodear hex sueltos en las reglas de componentes. Mobile-first con `@media (max-width: 768px)` (columna única en CTAs y en el grid de "qué incluye", tamaños de fuente reducidos). Añadido `<meta name="robots" content="noindex, nofollow">` para que no se indexe mientras es una página de pruebas. Comentario HTML al principio del archivo marcando explícitamente que es una landing de pruebas, con copy pendiente de validar, y remitiendo a esta entrada de `estado_proyecto.md` para el contexto. No se ha tocado `src/App.js`, ningún componente de `src/`, ni `package.json` — no se instaló ninguna dependencia (nada de React Router).
- **Build/lint:** OK — `npm run build` (CI=true) compila con "Compiled successfully.", sin warnings de ESLint. Confirmado que `public/landing-diagnostico-gratuito.html` se copia tal cual a `build/landing-diagnostico-gratuito.html` sin afectar al bundle de la SPA.
- **Pendiente:** (1) copy/contenido definitivo — el texto actual (título, subtítulo, bloque "qué incluye") es una primera versión razonable pero no validada por Marketing; (2) el usuario debe confirmar la URL final una vez exista una campaña de pago real (p. ej. si se sirve en `bytia.net/landing-diagnostico-gratuito.html` tal cual o se mueve a una ruta/subdominio distinto); (3) quitar el `noindex` si en algún momento se decide que la landing sí debe indexarse en buscadores.
- **Nota para futuros roles:** Marketing debería revisar y aprobar el copy (título, subtítulo, mensaje precargado de WhatsApp/email, bloque de "qué incluye") antes de usar esta landing en cualquier campaña de pago real — de momento es contenido de prueba, no definitivo. Diseño (cuando exista) debería revisar coherencia visual con la home (el layout es deliberadamente más simple que el Hero completo de `src/components/Hero`, sin grid decorativo ni stats, para mantener el foco en una sola conversión — confirmar si ese nivel de simplificación es el deseado o si conviene acercarlo más a la estética de la home).

---
